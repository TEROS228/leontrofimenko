'use client';

// Removed canvas animation — was running requestAnimationFrame every frame
// with no cancellation, consuming CPU constantly. Static CSS orbs replace it.
export default function FloatingShapes() {
  return null;
}
